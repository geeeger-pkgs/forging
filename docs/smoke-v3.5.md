# Forging v3.5.0 实机冒烟记录（2026-09-16）

> 起因：终审 A-M4 指出 §4 的 GUI 冒烟无留档、唯一证据链缺口。本页为当轮实机复现记录。
> 环境：生产构建（`npm run build` 后 `npm run preview`，:4173），Chromium（ZCode in-app browser），
> 视口 1280×800 + 375/390×720；构建字节数见 release-v3.5 §4。

## 关键路径与实测

| # | 步骤 | 实测结果 |
|---|---|---|
| 1 | 首屏（新档） | 顶栏「💰0 / 暂无进行中的动作」+ 导航 4 主项 + 教程卡「初识挖掘・挖掘铜矿石 ×10」+ 矿脉网格 + 右栏 tablist（装备/行囊/资源）全部渲染；无白屏/无错误覆盖层 |
| 2 | 点「铜矿脉」 | 动作确认框弹出（标题/产出/经验/稀有/∞ 无限/指定次数/＋队列位 300💰/取消/加入队列/开始） |
| 3 | 点「开始」 | 顶栏切为进行中：「铜矿脉 / 倒计时 / 停止」；产出累加（铜矿石 ×12，单价 2） |
| 4 | 教程「领取奖励」 | 金币 **0 → 20**；教程推进「初识挖掘 → 熔炼入门（制作铜锭 ×5）」 |
| 5 | 展开「▸ 更多」抽屉 | 8 工具（传承/任务/远征/图鉴/深渊/商店/成就/设置）；`aria-expanded` 正确；再点折叠 |
| 6 | 深渊面板文案 | 「可重复购买项的价格逐次上调（定向重铸券 ×1.3 / 永久速度 ×1.1）」——**空括号已消失**；公式行 `(10 + 2×层)`、`1 + ⌊最高层 / 20⌋`、`主题每 25 层循环、词条每 5 层轮换`；「每日可打满 24 次」；「入门 3 层之一」 |
| 7 | 图鉴未解锁分支 | 「每 14 天轮换 3 条长线任务」 |
| 8 | 传承面板 | 档位表 `50/60/70/80/90/100 → 2/6/12/20/30/42`；深造示例「迅捷精通 20 → 40 级」；**首点基准行「115d 3h」**（v3.5 新增披露） |
| 9 | 宽屏 1280×800 | 布局正常（三栏：导航 / 主区 / 右栏），截图落盘 （截图已清理，以省仓库体积） |
| 10 | 窄屏 375 / 390 | `document.scrollWidth === clientWidth`（375/375、390/390）——无横向溢出 |

## 覆盖边界（如实声明）

- 未覆盖：深渊挑战/扫荡、远征派遣、赛季任务推进、强化动作链、离线结算（均有单元/组件测试与既有 v3.4 冒烟覆盖；本轮为封板快速复验）。
- 截图：1 张（宽屏主界面）；会话截图原文件另存于 ZCode artifacts。

## 结论

关键路径可用，本批修复的文案全部实机复现正确；无 Blocker。
