# Forging v3.1 设计文档 ·「两位玩家测评的落地」

> 来源：`docs/review-v3.0-hardcore.md`（6.5）+ `docs/review-v3.0-ux.md`（6.8）+ 汇总 `docs/review-v3.0-two-players.md`
> 用户指令：「A/B/C 都需要改，另外，都做」→ 本文档记录**全部落地项**与其证据
> 版本：3.1.0 ｜ 存档版本仍为 **13**（新增字段全部走 ensureFields 幂等回填，无需迁移链）

## 1. A 深渊（两人共同点名）

| 项 | 落地 | 证据 |
|---|---|---|
| 入门三层 | `data/abyss.json` 新增 `introReqs [1.15, 1.5, 2.5]`（第 4 层起接 base 曲线） | 实算：T3 干净套(1.194)进第 1 层、T3+10(1.574)进第 2 层；**novice 仍 0 层**（保住目标带），early/mid/end 可达层 **9/24/34 完全不变** |
| 试炼层实质化 | 由"强化率权重 ×1.5"（实测仅 +1.3~1.9%，伪词条）改为**小试炼**：门槛 ×1.03、首通结晶 ×1.3 | `sim-abyss` 重跑：mid 在 8/13/18/23 层仍可过（不改变卡点） |
| 连打代价 | `chainCost [1,1,2]`：×1/×2 各 1 点、×3 花 2 点 | 面板显示代价；`tests/v31.test.ts` A3 断言"剩 1 点时 ×3 被拦下且不扣体力、×2 可过" |
| 结晶出口 | 永久速度上限 8→24、增长率 1.3→1.10 | `sim-abyss`：全清 11,812 → **21,510**（≈106 天 → **224 天**） |
| 补强指引 | 面板：`bestFloor=0` 给入门提示；落后时按**本层有效权重**列出贡献最低三项 | 实机截图核对（gap>0 分支与入门分支都验证过） |

## 2. B 经济与长尾

| 项 | 落地 | 证据 |
|---|---|---|
| 金币循环出口 | 新系统 `data/goldShop.json`（精华 100 金、重铸石 250 金，价格 ×1.02/次）+ `buyGoldShopItem` + 商店页区块 | 反套利由**内容校验**守护（买价必须 > 回收价）；`v31` B1 断言"任何次数下买价 > 回收价"与"金币不足阻塞" |
| 长尾压缩 | `levelCurve` 末段 1.02 → 1.01 | Lv100 −6.5%、Lv80→100 段 −12.9%（**Lv80 及以下锚点不变**）；`tests/level.test.ts` 锚点已更新 |
| 计数口径 | 新增 `totalEnhancesT4 / totalReforgesT4`，赛季"锤炼不止/精益求精"改指向它们 | `v31` B3：T1 强化只加总计数、T4 强化两计数都加 |
| 强化技能生效 | 每 10 级 +1% 强化成功率（Lv100 = +10%） | `v31` B2：同随机源下高技能成功次数 ≥ 低技能（且**断言样本非空**，避免死断言） |

## 3. C 首小时体验

| 项 | 落地 |
|---|---|
| 教程卡 | 目标文案（"制作 铜镐 ×1"）+ 进度 + **还差 N** + 「前往」（按配方技能推导去熔炼/锻造）|
| 目标卡高亮 | 对应动作卡描边 + 「教程目标」角标；卡面同时给出**输入 → 产出 · 耗时**（"12 铜锭 → 1 铜镐 · 2.2s"）|
| 回收可读 | 材料行显示单价；回收按钮仍保留（自动回收保留量仍用 prompt，未改） |
| 反馈补强 | 入队成功返回 notice；飘字先描深色边再填色 |
| 移动端 | 动作弹窗 footer 吸底（CSS 安全网）；390/375px 复查无横向溢出、无超宽元素 |

## 4. D 其余（部分）

- 装备预设 ×3（一键换装）：`meta.gearSets` + `saveGearSet/applyGearSet/deleteGearSet` + 行囊顶部操作行；
  装备已回收则跳过并如实提示；**不主动卸下**未在预设中的槽位。
- 章节二教程（8 步）：符文制作 / 重铸 / 远征 / 图鉴 5% / 深渊首战 / T4+ 强化 / 赛季等级 / 总等级 40；
  `checkTutorialStats`（主循环 + cmd 后检查）覆盖 stat/abyssFloor/codexPct/seasonLevel 四类目标，卡面文案与「前往」落点同步。
- 未做（记入 v3.2）：行囊折叠进主面板、工具抽屉、成就按接近度排序、强化列表过滤、赛季目标动态缩放、
  `acceptTutorial` 死命令清理、`settle.ts` 用 `Date.now()` 取符文增益。

## 5. 门禁与证据

- 测试 **376** 全绿（新增 `tests/v31.test.ts` 18 例）/ typecheck / build（gzip **103.3KB**，预算 ≤105KB）
- 证据脚本：`sim-abyss`（含 introReqs/chainCost）、`sim-audit`（栈审计 +3.284，250ms 下限仍不触底）、
  `sim-season`（图鉴里程碑改读表）、`sim-codex`、`report-content`、`audit-content` 全部重跑并落盘
- 实机烟测：教程卡→「前往」→目标卡高亮（锻造页显示"12 铜锭 → 1 铜镐 · 2.2s 教程目标"）、
  深渊连打代价与补强建议、金币商店、资源单价、390/375px 无溢出，**0 控制台错误**
