# Forging v3.2 设计文档 ·「UI/UX 集中优化」

> 来源：两位玩家测评中**全部未落地**的界面/交互项（`docs/review-v3.0-ux.md` 为主，`-hardcore.md` 补充）
> 用户指令：v3.2 集中做 UI/UX 的优化
> 原则：本版**不改数值**（唯一例外见 §4 的"顺带卫生项"，且都有测试守护）；一切改动都要能在窄屏与键盘下验证

## 1. 范围（四组）

### A 移动端 / 窄屏（测评 B-10、F）
| # | 问题 | 方案 |
|---|---|---|
| A1 | 导航把 4 技能 + 8 工具 + 静音 + 教程卡全部平铺，390px 下吃掉半个首屏 | 工具区收进**「更多」抽屉**（点击展开/收起，带 aria-expanded）；技能区与静音常驻 |
| A2 | 右栏（装备/资源/行囊）永远沉在整页最底部 | 窄屏下改为**底部悬浮 Tab**（装备/资源/行囊三键切换，默认收起；点击展开为面板） |
| A3 | 弹窗长内容时主按钮可能滚出屏 | 已做（v3.1 footer 吸底）→ 本版补：**深渊权重行折叠时给"已折叠 N 项"提示** |
| A4 | 顶栏在窄屏占位大 | 窄屏隐藏"总价值"与"总等级"，保留金币/动作/停止 |

### B 信息层级与可读性（测评 B-A/B）
| # | 问题 | 方案 |
|---|---|---|
| B1 | 材料行 4 个回收按钮 + 系统 `prompt` 设保留量 | 回收收进 **「…」菜单**（回收1/×10/全部/自动）；保留量改**内联数字输入** |
| B2 | 每个槽位常驻「卸下」按钮 ×10 | 移入**装备详情弹窗**（点击槽位打开），槽位行只留名称 |
| B3 | 装备总加成是 11px 灰字三行 | 提到 12px，并按**当前视图**高亮相关项（挖掘页亮挖速/产量…） |
| B4 | 时间/百分比单位混用（8h / 4.0 小时 / 14 分 33 秒 / 74% / 8.1%） | 统一：**时长**用 `Xs`（<60s）/ `Xm Ys`（<60m）/ `Xh Ym`；**百分比**一律一位小数 |
| B5 | 顶栏「停止」用危险色、视觉权重最高 | 降为描边按钮（hover 才显红） |
| B6 | 成就 82 项平铺、无进度 | 按"接近完成度"排序 + 每项显示 `当前/目标` |
| B7 | 强化列表平铺、找可强化件费眼 | **未满级优先** + 显示"材料够 N 次" |

### C 交互细节
| # | 问题 | 方案 |
|---|---|---|
| C1 | 键盘 Tab 焦点不可见 | 全局 `:focus-visible` 描边样式 |
| C2 | 图鉴分区要点开才知道差什么 | 默认展开**进度最低**的分区；分区行显示"还差 N 条" |
| C3 | 空态文案不统一（"暂无"/"暂无预设。"/"无所事事……"） | 统一为「暂无 X」句式 + 一句下一步提示 |

### D 顺带卫生项（不影响数值）
| # | 项 | 方案 |
|---|---|---|
| D1 | `acceptTutorial` 是空操作死命令 | 删除命令与联合成员（测试同步） |
| D2 | `settle.ts` 用 `Date.now()` 取符文增益 | 透传 `simulate` 的 `now`（与 v3.0 C12 同做法） |

## 2. 明确不做（记入 v3.3）
- 效率符文在线口径（v2.1 登记的既有取舍，改动要重跑经济审计）
- 赛季目标动态缩放（数值改动）
- 行囊/动作预设的拖拽排序、图鉴搜索全文检索

## 3. 验收标准（DoD）

> **核验历程（如实记录）**：第一轮 DoD 复核（375/390/1200px 实机 + 源码契约测试）勾选时发现 3 处不达标（见下"第一轮核验发现"）。
> 随后进入 ⑥ 资深玩家评审（`docs/review-v3.2.md`），两名独立评审员又抓出 **1 Blocker + 6 Major**，
> 其中一条明确指出"B1~B7/C1~C3 全部落地"当时**不成立**（卸下不可达 / B3 死选择器 / B4 只有 helper 没有集成 / B6 部分未兑现 / C3 残留）。
> 修复与复验见 `docs/review-v3.2.md` §2；处置后两名评审员独立复审（7.5 / 8.0，共同放行条件 N1 已修），修正与复验见 §4、`docs/smoke-v3.2.md`；下面每条都附**可复查证据**。

- [x] 390/375px：无横向溢出；工具抽屉可开合；右栏 Tab 可切换且默认不遮挡主内容
  - 375px：`scrollWidth == innerWidth`；抽屉默认收起（`display:none`、`aria-expanded=false`），点「更多」展开 8 个入口、选中任一工具后自动收起（实机）
  - **底部 Tab 条常驻视口底部**（`position: fixed`）：未滚动时 `top=667 / bottom=720`（视口 720）——第一轮用 `sticky` 时它只在页面底部可见，等于没有入口（评审 Major，已改）
  - 右栏默认零 section 可见（不占高度）；点 Tab 切换分区（窄屏展开后自动滚动到该分区，见下方评审 N1），再点收起；桌面 1200px：三区分显、Tab 条隐藏、工具区始终全展示
- [x] 键盘：Tab 可见焦点；工具抽屉与右栏 Tab 可键盘操作（aria-expanded / aria-selected）
  - 焦点环 `:focus-visible { outline: 2px solid var(--c-accent) }` 生效（实测 `outline-style: solid`）；已去掉旧版自带的 `border-radius:4px`（会让按钮聚焦瞬间抖动）
  - **Space 路径实测**：抽屉「更多」开合、右栏 Tab 切换，`aria-expanded`/`aria-selected` 同步；补 `aria-controls` + `role="tabpanel"`
  - 如实登记：本机自动化两条注入路径（Playwright `press('Enter')`、CUA 原始按键）的 Enter 均不触发原生 button click，**Enter 路径无证据**；控件均为原生 `<button>`（无 `tabindex=-1`、无 `preventDefault`），真实浏览器按平台语义应可用
  - 新交互点（槽位）已补 `role="button"` / `tabindex=0` / Enter；材料名点击尚未键盘化（登记 v3.3）
- [x] B1~B7/C1~C3 全部落地，且**没有任何系统 prompt/confirm 用于日常操作**（确认框仅保留破坏性操作）
  - "全部落地"在评审时**不成立**（B2 卸下不可达、B3 选择器写错、B4 未集成、B6 部分未兑现、C3 有残留），修复后才成立——逐条对照见 `review-v3.2.md` §2
  - 零系统弹窗：源码守卫（`src/ui` 无 `window.prompt(`，`window.confirm(` 仅 Prestige/Settings/RightPanel 且 RightPanel ≤2 处）+ 实机（点「存配装」→ 无对话框、出内联输入 → 输入 + Enter → 无对话框）
- [x] 时长/百分比格式统一（新增 `fmtDur`/`fmtPct` 并在测试中断言边界：59s/60s/60m/1h）
  - `tests/format.test.ts`：0/负数/NaN/∞ 兜底、<10s 一位小数、59s、**59.9s → "1m 0s"（进位修正）**、60s → "1m 0s"、3599s → "59m 59s"、3600s → "1h 0m"；`fmtPct` 一位小数与 `—` 兜底
  - **集成已完成**（评审 Major：此前只测了尺子没量身高）：`icons.ts` 改为转出口 `format.ts`，ActionDialog / MainPanel / ProgressBar / Expedition / Offline / Tasks / Codex 全部迁移；守卫测试禁止在 `format.ts` 之外自带 `toFixed` 取整
- [x] `npm test` 全绿（421，DoD 原定 ≥390）｜typecheck｜build｜实机烟测（桌面 + 390/375 + 键盘路径）
  - 421 项 / 24 文件全绿（含 `tests/v32-review.test.ts` 的评审处置回归 29 项）；`vue-tsc --noEmit` 无错；`vite build` gzip **106.39KB**（≤110KB 修订预算）
  - 实机：375/390/1200 三档布局 + 抽屉开合 + 底栏常驻 + 右栏 Tab 切换 + 卸下可达 + 材料菜单行内 + 键盘聚焦

### 第一轮核验发现（DoD 复核本身抓出的 3 项）
1. **窄屏抽屉默认态写反**（`NavBar.vue`）：初值 `!TOOL_VIEWS.includes(view)` 在技能页为 true → 窄屏默认展开。修为 `TOOL_VIEWS.includes(store.ui.view)`，并加源码契约回归测试
2. **`audit:content` 误报长期挂账**：T4 计数器用 `X = (X ?? 0) + 1` 写法，静态规则只认 `X++`/`X +=` → 报"从未累加"；已扩展规则 + toolchain 测试收紧为"零发现"
3. **审计报告版本号硬编码 `3.0.0`**：改为读 `package.json`

> **体积预算修订（如实登记）**：设计时定 ≤105KB，v3.2 实测 106.08KB（本版 UI/UX 代码 + 评审修复约 +1.5KB gzip）。
> 按项目纪律这属**预算变更**，故在此显式登记：**v3.2 起预算调整为 gzip ≤110KB**，理由是本版为 UI/UX 专版且新增项均有明确玩家可感知收益；
> 若 v3.3 无同类申诉，预算不再上调。
