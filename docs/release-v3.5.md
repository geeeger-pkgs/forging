# Forging v3.5 封板检查单（草稿）

> 用户判据：**未处置清单归零**（每条要么完成，要么正式裁定并写进本文件的「已知取舍」）。
> 里程碑：v3.5 只做 终验七维复核 → 全量回归 → 发布文档 → 最终评审，不新增玩法。

## 0. 3.4.1（终审判定：Blocker 当版清零）

| # | 项 | 来源 |
|---|---|---|
| 1 | 传承点数曲线改 steps²+steps（先出 D/F 段证据再定表）——首点 2763.6h/3.58 年一轮不可接受 | 终审 Blocker |
| 2 | 赛季档位改按 bestSkillLevel（生涯最高）取档，消除「刚传承即被锁 0.34」 | 终审 Major |
| 3 | 传承面板解锁前显示首点门槛与档位表（披露缺口） | 终审 Major |
| 4 | 重拍 v34-mobile-junior.png、补 smoke S2 实读文案、订正 §1.5 口径 | 终审 Major + 第 5 次宣称≠实现 |

## 0.5 v3.4.4（第三轮双人评审的全应用扫描发现，14 项）

详见 docs/review-v3.4.md 的「第三轮双人评审（v3.4.3）」一节：
- 玩家可见：strong describe.ts 强化率/经验/稀有 三处口径、面板强化率未含符文、商店 T5 旧数字、深渊 x1.3、成就两处口径、设置页版本号、教程前往落点（第 4/7/9 步）、队列位不标价、留 0 语义只在 title、远征补给预览口径；
- 工程：硬编码与内容表脱节（essencePerFour / 14 天 / x1.3）、产物与生成器 version 漂移。

**纪律升级（本线第 6 次同类问题的根治）**：凡界面/产物中的数字，必须有**数字级**断言；语义断言不算。

**纪律升级 2（v3.4.5，「探针绿」事件）**：新断言必须**先过自探针**才算完成——
"篡改实现对应处 → 该断言变红 → 恢复"。变绿的断言一律视为未生效，
典型失效模式：`if (def) expect(...)` 之类守卫让断言空跑、只断言"上升"不锁定精确值。
v3.4.5 已按此标准把 ①② 两条重写为精确值断言（探针：去 stoneFind 实参 → 0.007 vs 0.0084 红；
`abyssCrystals` 改恒 0 → 0 vs 7 红），当前 ①②③④ 探针 4/4 变红。

**纪律升级 3（v3.4.5，工具习惯）**：源代码修改一律用编辑器工具（Read/Write/Edit）直接落盘；
脚本只用于**生成物**（sim 输出、内容表生成）与**探针**（临时篡改-恢复，需带前后计数验证）。
此前用一次性脚本"插入"源码导致多次静默 no-op（宣称≠实现 的根因之一）。
**生效起点（v3.4.6 证伪复评 B 要求补记）**：自 `fc40ec0` 起；同批更早的 `8906bca` 仍是补丁脚本改源码。

## 1. 待清（来自 v3.4 六审的遗留登记）

| # | 项 | 来源 | 处置（v3.5 封板状态） |
|---|---|---|---|
| 1 | 里程碑奖励说明仅 `title`，触屏不可达；赛季轮换无预告 | 四审 U5 | ✅ **已满足**：里程碑奖励与状态已常显（CodexPanel 行内展示金币/精华/徽记 + 已发放/可领取/未达成）；赛季档位缩放有明细 + 预告「本季目标已冻结至轮换，下赛季按届时进度重算」 |
| 2 | `scripts/sim-season.mjs` 的缩放在脚本侧仍是内联拷贝（脚本无法 import TS） | 六审 | ✅ **已登记**：脚本头补同源说明（2026-09）；兜底为 tests/season.test.ts S12 逐字段断言 + tests/toolchain.test.ts 工件同源检查 |

## 2. 已知取舍（正式裁定，逐条给理由）

> 判据（用户）：未处置清单归零 —— **每条要么完成，要么正式裁定并写进本文件**。
> 26 行总表见 `docs/design-v3.4.md` §3；下表为"完成项证据落点"+"裁定组理由"，两表合起来即归零证明。

### 2.0 完成项的证据落点（26 行总表的"v3.4/v3.5 做"组）

| #（总表） | 条目 | 证据落点（可复核） |
|---|---|---|
| 1 | A1 效率符文在线口径 | `src/game/stats.ts#effectiveEfficiency`；settle 与 RightPanel 同源；`docs/sim-audit-output.json` A 段 |
| 2 | A2 Lv76~100 里程碑 | `data/levelCurve.json` + `aggregateEquipment` 单源；F 段证据；SAVE_VERSION 15 + 迁移回归 |
| 3 | A3 赛季 T2 证据 | `sim-season` T2 stage + `scaleByMaturity/coefFloor` 落 JSON；S12 逐字段断言 |
| 6 | A6 传承快轮回 | 点数 `steps²+steps`；D 段 `stretchScan`（快/满比 0.00×，≤1.25 判据） |
| 9 | B3 组件矩阵 5 缺口 | `tests/components/`（reachability / tutorial-landing / panel-copy-numbers） |
| 10 | B4 重掷确认 | TasksPanel 二次确认（组件用例：确认出现/取消不扣金） |
| 12 | C1 Lighthouse | `docs/lighthouse-v3.5.{json,html}`（本版复跑，见 §6） |
| 13 | C2 内容迁移规则 | 见 §5（规则 + 迁移链测试） |
| 16 | C5 cue 类型 / E3 burst | `scripts/audit-fx.mjs` + fx-map 闭合联合（v34-guards 断言） |
| 17 | D1 路线图行状态 | `docs/08-roadmap-v3.md` 收口更新 |
| 18 | D2 DoD 勾选 | `design-v3.0.md` / `design-v3.3.md` 订正 |

### 2.1 裁定"不做 / 保留"组（逐条理由）

| 条目标签 | 裁定 | 理由 |
|---|---|---|
| B1 回收撤销 | 不做 | 每命令即时落盘 + `totalGoldEarned` 单调计数 + Toast 无动作模型，硬做会引入"进度入账两次"；已有二次确认 |
| B2 未读/可领状态 | 不做 | 任务/图鉴/赛季奖励均自动发放，无"待领取"态；徽标只对真有该语义的远征/教程 |
| B5 三段教程 | 不做 | 章节二 8 步已覆盖符文/重铸/远征/图鉴/深渊/赛季/总等级 40 |
| 新技能线（采集/炼药） | 不做（方向声明） | `design-v3.0.md` 登记为 v3.x 首选扩张方向；需新技能/配方/矿场/平衡整套，封板版不扩，作为方向声明保留（非挂账） |
| 图鉴未收集图标/来源标签、行囊折叠、拖拽排序、图鉴检索、底栏 sheet | 不做 | 收益/成本不划算，或等真机反馈 |
| A5 赛季标签简化 / A4 回收阈值再校准 | 保留 | 如实标注优于隐藏；阈值刚由 v3.3 评审校准，无新证据不动 |
| C3 automation craft-only / C4 实例回收 60% | 不做 | 设计如此，注释与设置页已说明作用域 |
| E1 iOS 真机 / E2 软键盘×底栏 | 声明取舍 | 无真机验证条件；软键盘弹出时底栏可能遮挡（已登记，桌面/窄屏已验证） |
| E3 Enter 自动化路径 | 保留 | 键盘可达性以 Tab/Enter 主路径实现，自动化端点未做（设计取舍） |
| E4 后台静音 | 保留 | 页面切后台不强制暂停音效（浏览器策略差异，已登记） |
| E5 时间前拨上限 | 保留 | 前拨按算法补算产出、无硬上限（单机放置的设计选择） |
| E6 离线回体放宽 | 保留 | 离线上限 24 > 在线上限 12（有意奖励放置） |
| E7 性能口径降级 | 保留 | 低端机降级策略未做；当前 Lighthouse 99 达标，仍随版本记录 |
| E8 词缀盐残余风险 | 保留 | 存档盐值可见 → 词缀可提前推算（单机无对抗性，接受） |
| 传承早期停点效率 | 声明取舍 | 点数只依赖最低技能 → 提前传承换"即时小额精通"而非效率最优（见 `design-v3.4.md` §1.5） |

### 2.1 v3.4.5 双人确认评审的裁定（v3.4.6）

| # | 项 | 来源 | 裁定 |
|---|---|---|---|
| 1 | 文案守卫只覆盖 `×N / N天 / vN.N / 每N层条套级 / N+×层` 五类形态；其余数字不扫 | B-F4 | **接受（范围）+ 已收口（示例）**：形态外的数字不做全自动提取（误报会淹没）；`panel-copy-numbers` 现有 12 条值级断言。复评 B 探针点名的两处已接线：EnhancePanel 降级分界（enhance.json 推导）、ShopPanel 词缀条数区间/池大小（affixes.json 推导）。新增 UI 文案时按纪律补值级断言 |
| 2 | codexPercent 面板 `24.8%`（toFixed(1)）与成就按 `Math.round` 达标的 0.5pp 窗口 | B-Nit | **接受**：只会"提前 ≤0.5pp 解锁"，不会"显示达标却锁着"；若要动，须同时改面板与判定（改动面 > 收益） |
| 3 | 教程第 10 步（`stat/totalReforges`）落在锻造页，重铸入口在装备详情里（无高亮引导） | B-Nit | **接受（订正理由）**：教程卡实际文案为「重铸词缀 ×1」；`gotoStep` 对 stat 类只切视图、不打开右栏，玩家需自行点右栏装备行 → 详情 → 重铸（≥2 击）。重铸非阻塞，高亮引导留待下个大版本。（原裁定理由"文案已写'打开装备详情'"系虚构，复评 B 证伪后订正） |
| 4 | `b050be2` 提交信息「6 处硬编码」中 1 处是 MainPanel 的 HTML 开发注释（非玩家可见） | B-F7 | **记录**：计数口径确为"5 处玩家可见 + 1 处注释"，非虚报 |
| 5 | 纪律 3（源码用编辑器工具）的生效起点 | B-F8 | **修正**：自 `fc40ec0` 起生效；同批更早的 `8906bca` 仍是补丁脚本（已在纪律 3 正文注明） |
| 6 | codexPercent 的 `isMet` 取整分支无独立断言（value 侧有） | A-Minor-1 | **补**：见 v3.4.6 批次（边界夹具 24.5% 窗口） |

## 3. 体积预算放宽（用户指示）

用户明确指示"不必为 JS 体积膨胀做取舍"。据此：
- **gzip 预算不再作为取舍依据**（v3.2 登记的 ≤110KB 仅作参考，不再作为 DoD 门禁）；
- 原则改为**正确性与可读性优先**（文案数字一律从内容表插值；审计与守卫代码可以更长）；
- 仍保留 build 成功 + 记录体积作为回归观察（异常增长时复核，但不阻断）。

## 4. 封板验收记录（2026-09-16 · v3.5.0 · 全部实跑）

| 项 | 结果 |
|---|---|
| 全量测试 | **543 passed / 32 文件**（含文案守卫 3 pass、面板值级断言 12 条、迁移 13 + persist 20） |
| typecheck | `vue-tsc --noEmit` 退出码 0 |
| build | 成功；dist：JS gzip **110.21 KB** / CSS gzip 5.91 KB（体积仅记录，非门禁） |
| 三审计 | audit-content「未发现不可达内容」；sim-audit「无显著倒挂（0.00× ≤1.25×）」；sim-season「承诺集最差占比 99% ≤100% ｜ 严格递增 ｜ 下限保护」 |
| 存档迁移 | 1→15 全链（migration.test 13 项 + 真实档 fixture save-v13）；拒载未来版本；单调自愈 |
| Lighthouse 13.4.1 | performance **99** / accessibility **100** / best-practices **100** / seo **100**（生产构建 preview，见 §6） |
| GUI 冒烟（生产构建） | 首屏→开始挖掘（倒计时/停止）→教程领取（金币 0→20、教程推进「熔炼入门」）；抽屉展开 8 工具；**深渊/图鉴/传承三面板文案实机核对**（空括号已消失、×1.3/×1.1、25 层/5 层、20→40 级均正确渲染） |
| 评审链 | v3.4.5 双人确认（P0×5 必修）→ P0/P1/P2 三批清账 → P3 订正 → v3.4.6 **双人证伪复评**：A 独立重做 22 项探针、B 24 项，全部"改回去必红"；代码本体放行 |
| dev server | :5174 已关闭（封板）；preview :4173 可用 `npm run preview` 重启 |

## 5. 内容与存档迁移规则（C2 落地）

- **存档版本**：`SAVE_VERSION = 15`；`MIGRATIONS[n]` 为逐版纯函数（13→14 补 `meta.bestSkillLevel`、14→15 补赛季档位快照 `season.scale`），1→15 全链由 `tests/migration.test.ts` 断言；
- **拒载策略**：未来版本号或结构损坏 → `deserializeSave` 返回 null（不静默重置，护玩家进度）；
- **加载自愈**：`ensureFields` 对单调字段（bestSkillLevel 等）做不回退修正；
- **内容表变更**：先 `scripts/sim-*.mjs` 出证据 → `scripts/gen-content.mjs` 生成 → `tests/toolchain.test.ts` 断言"工件==实现" → `tests/copy-numbers.test.ts` 三轮守卫防文案漂移（`levelCurve.json` 为手写表，校验在 `validateContent`）。

## 6. 性能与体积口径（E7 的替代记录）

- **Lighthouse（生产构建）**：perf 99 / a11y 100 / bp 100 / seo 100；FCP 1.5s / LCP 1.9s / TBT 80ms / **CLS 0**；报告 `docs/lighthouse-v3.5.{json,html}`；
- **体积**：JS gzip 110.21 KB（v3.3 记录为 107.65 KB；增量来自文案插值与守卫相关渲染分支，用户已裁定**不作为取舍依据**，仅记录）；
- **低端机降级策略**：未做（E7 裁定保留）；后续如做，以本文件的口径记录为基线。
